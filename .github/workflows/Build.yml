name: Build
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: build docker container
      run: |
        docker build . -t stockfx
    - name: Upload math result for job 2
    - uses: actions/upload-artifact@v1
      with:
       name: Images
       path: Images/
